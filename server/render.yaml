services:
  - type: web
    name: proofpay-oracle
    runtime: node
    rootDir: server
    buildCommand: npm install; npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_VERSION
        value: 20.15.1
